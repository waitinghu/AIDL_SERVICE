<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seuic.keycode"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--         <receiver
            android:name="com.seuic.keycode.KeyCodeReceiver"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.odm.action.KEY_DOWN" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.seuic.keycode.KeyMappingRecordService"
            android:exported="false"
            android:process=":romote" >
            <intent-filter>
                <action android:name="com.seuic.service.KEY_RECORD" />
            </intent-filter>
        </service> -->
    </application>

</manifest>